server:
  port: 3377

spring:
  application:
    name: cloud-alibaba-nacos-config
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: DEV_GROUP
#        namespace:

management:
  endpoints:
    web:
      exposure:
        include: "*"